import React from "react";

interface PointSystemProps {
  points: number;
  setPoints: React.Dispatch<React.SetStateAction<number>>;
}

function Points({ points, setPoints }: PointSystemProps) {
  return <></>;
}
export default Points;
